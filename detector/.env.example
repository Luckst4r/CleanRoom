# Local Ollama needs NO API key — you can ignore this file entirely.
# It only matters if you point detector/config.yaml at a cloud backend that
# requires auth. In that case, copy this to .env and set the key below.
# VISION_API_KEY=your-key-here
